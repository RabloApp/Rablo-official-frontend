import React from "react";
import WorkSection from "./WorkSection/WorkSection";

const MainSection = () => {
  return (
    <main>
      <WorkSection />
    </main>
  );
};

export default MainSection;